# 2026-09-18
 - generalize the concept to an 'entity' for which several 'actions' can be started instead just "contractor's client" -> run applications in isolated environment tailored to clients working environment (git settings, ssh or other keys, ...) ?
    - what's there to generalize?
       - ux/ui to the point where dev defines handlers of button pressing
         and icons and content of 'add entity' and 'add action' dialog windows